{{- if .Values.envoyProxy.enabled }}

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute

metadata:
  name: {{ include "greeter.fullname" . }}

spec:
  parentRefs:
    - name: {{ .Values.envoyProxy.parentRefs.name }}
      namespace: {{ .Values.envoyProxy.parentRefs.namespace | default "envoy-gateway-system" }}

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

      backendRefs:
        - name: {{ include "greeter.fullname" . }}
          port: {{ .Values.service.port }}

{{- end }}
